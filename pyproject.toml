[project]
name = "pocket-ledger-budget"
version = "0.1.0"
description = "Original personal budget XLSX workbook generator"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]
